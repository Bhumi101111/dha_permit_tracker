{
  "name": "dha-permit-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "DHA Permit Tracker — single-service deploy (Express serves the built React app)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build && npm --prefix server run seed",
    "start": "npm --prefix server start"
  }
}
